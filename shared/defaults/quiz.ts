import type { Difficulty, QuizMode } from '../types/enums.ts'

export const DEFAULT_TIME_LIMIT = 600
export const DEFAULT_PASS_SCORE = 80
export const DEFAULT_RANDOM_COUNT = 0
export const DEFAULT_MODE: QuizMode = 'exam'
export const DEFAULT_DIFFICULTY: Difficulty = 'keep-question'
